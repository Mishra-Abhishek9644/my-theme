<!doctype html>
<html>
  <head>
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    {{ 'main.css' | asset_url | stylesheet_tag }}
  </head>
  <body>
    {% section 'header' %}

    <main>
      {{ content_for_layout }}
    </main>

    {% section 'footer' %}

    <script src="{{ 'main.js' | asset_url }}" defer></script>
  </body>
</html>
